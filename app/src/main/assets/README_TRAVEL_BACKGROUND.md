# China travel background

The three `china_travel_bg_0*.b64` files contain an optimized WebP rendering of the user-provided China travel poster. `TravelBackground.kt` concatenates and decodes them at runtime.